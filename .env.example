# 관리자 수퍼계정
ADMIN_EMAIL=admin@gamyeon.com

# 평문 비밀번호 (ADMIN_PASSWORD_HASH가 없을 때 fallback으로 사용 — 개발 전용)
ADMIN_PASSWORD=your-dev-password

# bcrypt 해시 비밀번호 (프로덕션 권장)
# 생성: node -e "const b=require('bcrypt'); b.hash('your-password',12).then(console.log)"
ADMIN_PASSWORD_HASH=

# JWT 설정
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_EXPIRES_IN=8h
JWT_REFRESH_SECRET=your-jwt-refresh-secret-key-change-in-production
JWT_REFRESH_EXPIRES_IN=14d
ALLOW_MOCK_ADMIN=false

# 서버 포트
PORT=3002

# 프론트엔드 URL (CORS origin)
FRONTEND_URL=http://localhost:3001

# Database (PostgreSQL)
# 공용 dev DB 기준: PostgreSQL / DB명 gamyeon
# 스키마 생성 및 마이그레이션은 Spring이 담당, 백오피스는 같은 DB를 read/write로 사용
AWS_REGION=ap-northeast-2
DB_SECRET_ID=
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=gamyeon_backoffice
DB_PASSWORD=change-me-backoffice
DB_DATABASE=gamyeon
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=true
DB_SYNCHRONIZE=false
DB_LOGGING=false
DB_MIGRATIONS_RUN=false

# S3/스토리지 설정 (이미지 업로드)
S3_REGION=ap-northeast-2
S3_BUCKET=gamyeon-backoffice-dev
S3_ACCESS_KEY_ID=your-access-key
S3_SECRET_ACCESS_KEY=your-secret-key

# S3 호환 스토리지(MinIO 등) 사용 시 지정
S3_ENDPOINT=
S3_FORCE_PATH_STYLE=false

# 파일 공개 URL base (CDN/커스텀 도메인)
S3_PUBLIC_BASE_URL=

# Consul 서비스 디스커버리
CONSUL_HOST=localhost           # 로컬: localhost / EC2: consul (컨테이너 이름)
CONSUL_PORT=8500
CONSUL_SERVICE_NAME=GAMYEON_BACKOFFICE_SERVER
CONSUL_SERVICE_ADDRESS=localhost  # 로컬: localhost / EC2: backoffice-api (컨테이너 이름)

# Consul KV 환경변수 로딩 (앱 시작 시)
CONSUL_KV_ENABLED=true
CONSUL_KV_PREFIX=backoffice-server/
CONSUL_KV_OVERRIDE=true
CONSUL_KV_TIMEOUT_MS=3000
CONSUL_KV_REQUIRED=false
# ACL 사용 시 토큰 지정
CONSUL_HTTP_TOKEN=
